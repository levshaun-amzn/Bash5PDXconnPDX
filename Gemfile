source 'http://rubygems.org'
gem 'sinatra', '~>1.4.0'
gem 'json', '~>1.8.0'

# These are the dependencies that are used only for unit tests.
group :test do
  gem "rspec", "~>3.5.0"
  gem "rack-test", "~>0.6.0"
end